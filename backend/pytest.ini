[pytest]
asyncio_mode = auto
# Governs async *fixtures* only — the module-level pytestmark in
# tests/test_health.py is what actually pins the three tests to one shared
# event loop (see that file's docstring for why that matters).
asyncio_default_fixture_loop_scope = session
pythonpath = .
